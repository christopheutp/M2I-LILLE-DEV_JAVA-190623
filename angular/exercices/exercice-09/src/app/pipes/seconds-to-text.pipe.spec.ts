import { SecondsToTextPipe } from './seconds-to-text.pipe';

describe('SecondsToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
